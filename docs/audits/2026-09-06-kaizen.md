# Accounting engine Kaizen — 2026-09-06

Scope: canonical www checkout; software validation and reference integrity.
Reporting basis: not applicable to this tooling audit. No ledger, rate,
statutory source state or professional accounting judgement was changed.

## Observed defects and repairs

The strict native gate initially failed one obsolete Digital Research link.
Its aggregate function separately returned `pass` for empty results, unknown
states and a child process reporting `pass` with exit code 1. Those synthetic
negative probes now return `fail`; ordinary pass/caveat/fail precedence is
preserved. Ten regression cases run in the principal gate.

The canonical parser additionally found 54 invalid YAML headers, despite the
native field-name regex reporting no contract errors. Only their description
scalars were quoted; words, domain rules and bodies were preserved. A real
YAML parser now checks mapping, identity and non-empty description across all
108 active files, and rejects an empty catalogue. Python/PyYAML are explicit
requirements in CI and the README. No automatic dependency installation was
performed on this machine.

Active doctrine and quality-gate references no longer instruct consumers to
mirror the finance engine. The old how-to layout is explicitly inactive
migration history; consumer adoption and professional-review obligations
remain. Corrected the Kaizen link and removed a conflicting hardcoded count.

## Acceptance evidence

| Check executed locally | Result |
|---|---|
| `tools/validate-doctrine.ps1 -Strict` | All 12 checks pass, zero findings |
| Accounting invariant fixture | 63 assertions, zero failures |
| Aggregation regression | 10 synthetic cases pass |
| `python -m unittest discover -s tests -p 'test_*.py'` | 3 tests pass, including YAML negative cases |
| Canonical syntax inventory | 54 frontmatter errors to zero; 108 active skills |
| Canonical full portable contract scan | 1/108 fully compliant; broader adapter/capability contracts remain gaps |

The local contract and canonical portability contracts are different. The
native pass must not be presented as portable compliance: the canonical scan
still reports capability 104, degraded-mode 103, anti-pattern 14, metadata 14,
section 21 and trigger 29 exceptions. These counts are structural observations,
not semantic scores. They require domain-specific migration, not bulk boilerplate.

The [independent code review](2026-09-06-kaizen-independent-review.md) records
the aggregation recheck and any parser follow-up. CI was updated but remote
execution is NOT ASSESSED because nothing was pushed.

## Output and improvement boundaries

Ledger/posting and reversal/duplicate/source-state teaching fixtures were
executed locally. IFRS/IPSAS/tax guidance, financial statements, close and
consolidation, budgets/costing, controls/audit packs, integrations, continuity
and finance UX have routes but no new client/runtime/render/professional
acceptance evidence in this cycle. All such outcome readiness is NOT ASSESSED.
Source-register shape is tested; live statutory correctness is not. No numeric
whole-engine grade is derived from the structural pass. Policy cap: 65/100;
improvement target: 95/100, not achieved or certified here.

P0: retain fail-closed parser/aggregation checks and canonical references.
P1: migrate the outstanding portable contracts individually while preserving
accounting invariants; retain one source-to-ledger-to-report re-performance
with independent controller acceptance. P2: expand rendered/export and
continuity evidence for the remaining output families. No new skill needed.

Owner proposed: engine maintainer plus controller for finance acceptance.
Re-audit: 2026-09-13, or upon schema, source or doctrine changes. Stop if missing
evidence produces a pass or a repair weakens a control. Roll back only the
reviewed patch and rerun the negative fixtures; preserve user work and
historical evidence. Changes remain local and uncommitted.
